# 修订对照清单（完善版 → 修订版）

对照对象：kiki 上传的《竞赛论文_完善版.docx》（下称“原稿”）与本次生成的《竞赛论文_修订版.docx》（下称“修订稿”）。修订稿源文件为 `paper/v3/*.md`，数字全部由 `paper/v3/tools/std_caliber.py` 从 `results/` 已有记录计算，未改动 `solution/`、`results/` 与评估器，未重新求解。只列实质性修改；纯措辞调整见第六部分的规则。

## 一、结构调整

| 编号 | 原稿 | 修订稿 | 理由 |
|:--|:--|:--|:--|
| S1 | 第 1 章问题背景与重述；1.3 问题分析下分 1.3.1～1.3.3；1.4 技术路线下分 1.4.1～1.4.4 | 第 1 章“问题重述与问题分析”：1.1 背景、1.2 重述、1.3 分析（三个问题对比表 + 三段分析，不设小节）、1.4 技术路线（一段 + 路线图） | 按要求去掉过细的小节，用表格说明三个问题的差别 |
| S2 | 第 2 章模型假设、第 3 章符号说明（DDR 公平共享、核内调度固定等写成“假设”） | 第 2 章“基本规则、模型假设与符号”：2.1 评估规则与硬件规则（7 条，题目或评估器给定）、2.2 模型假设（3 条，为求解所作简化）、2.3 符号 | L6：区分“题目给定的规则”与“建模假设”；DDR 公平共享、核内调度固定、只读 L2 语义等是评估器行为，改列为规则 |
| S3 | 第 4 章“评估机制解析与统一优化模型”，含 4.3 测试数据结构特征 | 第 3 章“统一优化模型”：决策变量 → 约束 → 字典序目标与复杂性 → 影响模型结构的评估机制 → 同步层次构造 → 模型层次与下界；结构特征统计移到 7.8 | 按“决策—约束—目标—机制—构造”的顺序展开；数据统计属于检验内容 |
| S4 | 第 5 章问题一：问题分析 → 求解思路 → … → 本章小结 | 第 4 章问题一：问题特点 → Task 级时序模型 → 粒度折中 → CAP-LS（S1～S4 + 算法 4-1）→ 补充搜索与评估次数控制 → 复杂度 → 标准求解流程与结果口径 → 结果 | 按“问题特点 → 新增模型 → 求解算法 → 结果”展开；新增 4.7 定义结果口径 |
| S5 | 第 6 章问题二：问题分析 + 求解思路 + 执行模型 + 算法改进 + 耦合分析 + 结果 + 小结 | 第 5 章问题二：只写相对问题一的变化（执行模型、$(\lambda-1)$ 割、表调度与采样、结果） | 避免重复第 4 章已有内容；删去“粒度、驻留与通信的耦合”一节（内容并入 5.1 与 7.4） |
| S6 | 第 7 章问题三：命中模型 → 代价 → 灵敏度 → 结果（Cache 加速比一个口径） | 第 6 章问题三：命中机制 → Cache 感知代价 → 容量与带宽 → 结果；Cache 加速比拆成“配置最优性能比较”和“纯 L2 硬件收益”两种口径 | 按要求拆分两种口径，避免把求解差异算作硬件收益 |
| S7 | 第 8 章模型检验（基线、消融、灵敏度、下界、代理、方案来源、结构、六个优化方向对应表、小结） | 第 7 章模型检验：新增 7.1 统计口径说明、7.3“事后消融分析”、7.7“结果口径：标准流程与离线候选扩展”；8.8 改为 7.9“关键机制及其性能贡献”（机制—模型—实验依据表） | L1、L3、L4；按要求改写 8.8 |
| S8 | 第 9 章模型评价与推广（优点 4 条、缺点 4 条、推广 4 条、总结） | 第 8 章模型评价与总结：优点不重复数字、只指向证据；缺点 5 条写明失效条件；改进方向措辞收敛 | 按要求重写；删去“效果显著”“结果稳定”等自评 |
| S9 | 每章末“本章小结” | 删除 | 小结只重复正文，属于 AI 痕迹 |
| S10 | 附录 A～C | 附录 A（标准流程口径的逐用例表）、B（程序与复现，含 L5 说明）、C（核心程序节选）、D（命题 3-1 与推论 3-3 的证明） | 证明移入附录 D；附录 A 改用标准流程数据 |

## 二、结果口径与实验逻辑（L1～L7）

| 编号 | 原稿 | 修订稿 | 理由 |
|:--|:--|:--|:--|
| L1 | 最终方案 = “保底池”：标准流程候选 ∪ 消融、粒度、基线实验方案 ∪ $N-1$ 冠军嵌入 ∪ 问题二、三互投 ∪ 单核兜底，取最小者；表 8-5 列出各来源 | 主结果改为“标准求解流程”：预定义网格（按规模裁剪）+ 问题一退火 / 问题二、三采样，由评估器择优（`experiment.run_portfolio`，`results/main.csv`）。原口径改称“离线候选扩展后的最好结果”，只在 7.7 节单独报告，并说明它用到了检验实验产出。表 8-5 改为表 7-5“标准流程输出方案的来源”，离线严格更优的来源另述 | 检验实验的方案回流到主结果，结果无法由单一算法复现，且与基线比较形成循环 |
| L1-a | 问题一平均加速比（N=2～5）1.81、2.55、3.19、3.74 | 1.76、2.45、3.14、3.64 | 口径变化，数字全部来自已有评估记录 |
| L1-b | 问题二 2.06、2.88、3.63、4.24 | 2.05、2.86、3.58、4.14 | 同上 |
| L1-c | 问题三 2.06、2.89、3.67、4.31 | 2.06、2.87、3.65、4.21 | 同上 |
| L1-d | “每个核数下最小加速比不低于 1，逐用例随核数单调不降” | 删除该结论。标准流程下 case_016 有 8 个配置加速比略低于 1（最低 0.9992，差 0.08%），$\mathrm{MK}_N>\mathrm{MK}_{N-1}$ 分别出现 33、26、21 次；给出加入“$N-1$ 核嵌入 + 整图单核”两个候选后的推算值（问题一 1.76、2.45、3.15、3.67 等），并注明尚未写入程序 | 原结论依赖保底池中的嵌入与兜底，标准流程不具备该性质 |
| L2 | 求解时间取自主实验（端到端中位数 45.8 s 等），与离线口径的加速比并列 | 加速比与求解时间都来自标准流程同一次运行，表 4-4 注明“时间与上文的加速比来自同一次运行” | L2：性能与时间同口径 |
| L3 | “最终方案相对 B4 高 +36%、+30%、+30%，在任何用例上都没有输给它（99/1/0、81/19/0、82/18/0）” | 标准流程相对 B4：+34%、+28%、+29%，胜/平/负 97/2/1、79/19/2、80/19/1，Holm 校正 $p$ 重算；分开写“c2 与 B4（机制比较）”和“标准流程与 B4（完整方法比较）”；离线结果与 B4 的比较（+36% 等）注明“候选集包含基线本身，不宜作为算法优劣依据” | 原比较中离线池含 6/3/2 个直接取自基线的方案，存在循环 |
| L3-a | 未说明 Wilcoxon 检验的适用范围 | 7.1 节说明：100 个用例作配对样本，$p$ 值只说明差异在这批用例上是否一致，不能推广到用例集以外 | 按要求说明检验局限 |
| L4 | “消融实验确认同步层次分层与通信感知分块是两项核心机制” | “事后消融分析”：单独运行、不参与主结果选择，针对单一配置 c2，不代表多候选择优后的边际贡献 | L4 |
| L5 | 附录 B：“`solve.py` 只包含候选网格，不含退火、采样和保底池，其结果可能略低于正文” | 附录 B 给出 $N=4$ 的量化差距（网格择优 3.11、3.54、3.63 对标准流程 3.14、3.58、3.65），并附补丁 `paper/v3/tools/solve_standard_flow.patch`，使 `solve.py` 默认调用 `run_portfolio`；是否采纳标为【需要作者确认 / 代码层面调整】，两种写法都已给出 | L5 |
| L6 | 见 S2 | 见 S2 | |
| L7 | 统一建模中“目标函数与解析代理”混写 | 新增表 3-1“三类模型的区分”：A 原问题优化模型（上报结果）/ B 场景解析模型（解释机制、指导候选）/ C 搜索代理函数（内部排序）；正文在使用处标明类别（如式 4-7 的解析代理 $F$、问题二的事件模拟代理标为 C 类），并说明 B、C 两类不作为结果使用 | L7 |
| L8 | 算法流程中含“保底池汇总”“跨问题互投”步骤（算法 5-1、图 5-2、图 5-3～5-5） | 算法 4-1 与流程图去掉这两步；框架图注明“检验实验产生的方案不参与选择” | 与 L1 一致 |
| L9 | 问题二“以单核基准为界，换入换出由 9.63×10⁸ 降到 3.43×10⁸” 未说明对照基准 | 写明对照：“未加入单算子子图候选之前、只在 c0～c13 中择优的主实验”（快照 `results/baseline_snapshot/main_before_T11.csv`） | 按要求写清基准 |

## 三、强结论弱化（对应要求第 5 节的 11 项）

| 编号 | 原稿 | 修订稿 | 理由 |
|:--|:--|:--|:--|
| C1 | “证明按 (r, c) 编号后赛题的全部硬约束自动成立” | “（C1）～（C5）由构造保证”；引理 3-1 后补充：L1/UB 容量、流水线、带宽与 L2 语义（C6～C9）不由构造保证，由评估器在运行时处理 | 构造只保证结构性约束 |
| C2 | “搜索中无需判环 / 不需要判环” | “在保持层次单调性的候选生成过程中，无需反复进行商图环检测”；并注明实现中仍用 `check_monotone` 复核一次，模拟退火单独判环 | 退火会打破层次 |
| C3 | “搜索只在分核这一维上进行” | “CAP-LS 主流程把局部搜索集中在块级核心分配上，子图边界与执行层次主要由构造规则和有限的候选生成确定；候选网格另外覆盖粒度、层指派、子图规模上限等，问题一的退火还包含合并、拆分与换核” | 实际搜索不止一维 |
| C4 | “一套框架覆盖三个场景” | “三个问题共用一套流程”；“三个场景”统一改为“三个问题”或“场景 A、场景 B 及其 L2 扩展” | 题目只有两个硬件场景 |
| C5 | “跨核搬运量恰为以张量字节为权的 (λ−1) 超图割” | “对单生产核中间张量和图输入张量，跨核搬运可写成 (λ−1) 超图割”；式 5-4 下注明求和只对这两类张量进行 | 多生产核张量、图输出张量不满足 |
| C6 | “通过互投保证只读 Cache 配置不劣于无 L2”“问题三的冠军理应不慢于问题二” | 6.5.4 节：问题三不保证不慢于问题二，分两层原因（FIFO 时序效应：同一方案 3/400 更慢、最大 0.092%；求解所选方案不同：标准流程 24/400 更慢、最大 21.03%）；保留离线口径下 case_030、case_048 两个反例 | 按要求保留反例、取消“保证” |
| C7 | “场景 A 偏好粗粒度，场景 B 对粒度不敏感”“边界搬运随 q 单调增、换入换出单调减” | “在保持核内顺序与分核结构基本不变、逐级嵌套细分的近似条件下……通常……可能存在较优的粒度区间”；7.4 节注明结论只在 29 个用例、$N=4$ 上成立 | 单调性只是近似 |
| C8 | “题设的 1 MB、250 B/cycle 已接近饱和，收益上限由复读量决定” | “在 18 个用例、$N=4$ 的条件下，L2 容量与带宽在所测范围内未观察到明显的敏感性”；“一种可能的解释是……”；补充样本代表性（Cache 收益前 10 的用例只有 3 个在样本内，样本命中率 20.1% 低于全体 27.2%） | 子集结论不能外推 |
| C9 | “约 60% 的未命中……是 Cache 感知调度可以继续争取的空间” | “在 $N=4$ 的统计中，约 60% 的未命中字节来自并发首读与 FIFO 淘汰……是进一步优化空间”；并注明该拆分基于离线口径方案 | 限定统计条件与口径 |
| C10 | “冠军方案的换入换出减少 64%” | 写明对照基准（见 L9）与口径（标准流程） | 按要求写基准 |
| C11 | “29 个超线性用例中有 21 个属于这一类。其余……来源是单核启发式调度本身的损失” | 标准流程：27 个中 19 个单核基准有换入换出；其余 8 个“其来源可能是单核启发式调度本身的损失……这一解释是推断，没有逐用例验证” | 数字随口径更新，推断改为推断语气 |
| C12 | “问题二全面高于问题一”“CAP-LS 在三个问题上都显著更优且没有一个用例劣于最强基线” | “问题二在每个核数下都高于问题一”；与 B4 的比较给出负例数 | 口径变化后原说法不成立 |
| C13 | “逐行读懂官方评估程序”“以官方评估程序为唯一事实基准” | “以官方评估程序的输出作为最终评价口径” | 按术语要求 |
| C14 | 问题一额外搬运“以切分边界为主”（58.7%） | 标准流程方案中切分边界只占 32.8%，以换入换出为主；与问题一偏粗粒度的分析一致 | 口径变化后原结论不成立，按新数据改写 |

## 四、摘要

原稿摘要 1639 字（不含空白），修订稿 994 字，删减约 39%；由 6 段改为 5 段（问题与目标、统一模型与核心机制、问题一、问题二与三、检验与结论），PDF 预览中摘要页为第 1～2 页。

从摘要中删除或弱化的内容：

1. 独立的“主要创新”段（按要求删除；模型贡献在第 2 段按内容描述）。
2. “1500 个方案经官方命令行复核，0 处不一致”（该复核针对离线方案，标准流程方案尚未复核）。
3. 保底池、$N-1$ 嵌入、跨问题互投等来源细节。
4. 单用例数据（case_003 执行轨迹停摆时长等）。
5. “14～18 组候选”、“351/400 个配置在 10 分钟内”与求解时间分位数（只保留中位数）。
6. 消融的完整数字（只保留同步层次一项，并注明为事后消融、问题一为 96 个用例）。
7. “以官方评估程序为唯一的事实基准”→“以官方评估程序的输出作为最终评价口径”。
8. “按 (r, c) 编号”→“按层次与核心的字典序编号”。
9. “全部硬约束自动成立”→“算子覆盖、子图商图无环和同核顺序合法等结构性约束由构造保证”。
10. “跨核搬运恰为 (λ−1) 超图割”→ 限定为单生产核中间张量和图输入张量。
11. “L2 容量与带宽已接近饱和”→“在 18 个用例、$N=4$ 的子集上……影响不超过 0.2%”。
12. “最终方案逐用例单调不降、不低于 1”（标准流程不具备，删除）。
13. 相对 B4 的提升由 +36%/+30%/+30% 改为标准流程口径的 +34%/+28%/+29%。

## 五、图表与公式

| 编号 | 原稿 | 修订稿 | 理由 |
|:--|:--|:--|:--|
| F1 | 数据图取自离线口径 `final.csv` | 全部依赖主结果的图（加速比曲线、分布、超线性、搬运构成、P2/P3 对比、基线、下界、结构、热力图、汇总、执行轨迹 (c)）改用标准流程数据重画，存于 `figures/v3/` | 与 L1 一致 |
| F2 | 图“逐用例 Cache 加速比” | 改为“逐用例的纯 L2 收益”（同一方案在两个评估器下的比值） | 与 6.5.2 的两种口径对应 |
| F3 | 技术路线图含“官方命令行逐方案复核”、框架图含保底池 | 改为“结构特征与难例”；框架图去掉保底池，注明检验实验方案不参与选择 | 标准流程方案尚未做命令行复核 |
| F4 | 表 8-5 最终方案来源（含消融、基线、互投） | 表 7-5 标准流程来源 + 表 7-6 两种口径对照 | L1 |
| F5 | 公式 | 全部保留原式；原式（4-6）超图割移到第 5 章（式 5-4），式本身不变，式下加注求和范围；原式（4-7）下界、式（4-8）加速比分解保持原样，编号改为式 3-6、式 3-7 | 按要求不改公式 |
| F6 | 数字带千分位（如 237,543） | 去掉千分位 | 格式要求 |

## 六、术语与措辞（全文统一执行）

- 冠军方案 → 标准流程选出的方案 / 输出方案；保底池 → 离线候选扩展；跨问题互投 → 跨场景候选复用（仅在改进方向中作为建议出现）；兜底 → 代理筛选策略；兑现收益 → 反映到最终 Makespan 中；低一档 → 整体低于；继续争取的空间 → 进一步优化空间；官方 trace → 评估器执行轨迹。
- “本文”改为“本题 / 所提方法 / 所建模型”；“赛题”只在 1.1 首次出现时保留，其余改为“本题 / 题目”；首次出现“官方评估程序”后统一称“评估器”。
- 删去各章开头的“本章先……再……最后……”、章末小结，以及“首先/其次/最后”“第一/第二/第三”式的并列展开；删去“显著、高效、全面、核心、决定性、唯一事实基准”等评价性词语（“稳定排序”“核心”作为术语保留）。
- 事实 1～5 改称机制 1～5；“官方 trace 证实”改为“在评估器执行轨迹上得到印证”。
