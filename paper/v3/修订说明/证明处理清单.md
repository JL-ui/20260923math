# 证明处理清单

原则：定理、引理的陈述与证明逻辑都不改动，只调整编号、所在位置与适用范围的说明；证明中引用的“事实 k”统一改为“机制 k”。原稿第 4 章的内容在修订稿中位于第 3 章，编号随之由 4-x 改为 3-x。

| 原稿编号 | 修订稿编号 | 修订稿位置 | 处理方式 | 理由 |
|:--|:--|:--|:--|:--|
| 命题 4-1（受限问题的判定版本 NP-完全） | 命题 3-1 | 陈述在 3.4 节；完整证明移到附录 D | 正文保留陈述与三行证明概要（PARTITION 归约的思路）；附录 D 给出完整证明，补写“不设容量、带宽与通信代价”的归约条件和“判定版本属于 NP”一句 | 该命题只用于说明精确求解不现实，不是方法主线；正文只需结论 |
| 定义 4-1（跨核同步层次） | 定义 3-1 | 3.6 节 | 原样保留，公式编号随章节调整 | 核心定义 |
| 引理 4-1（可行性） | 引理 3-1（构造可行性） | 3.6 节 | 陈述与证明完整保留在正文；“按 (r, c) 编号”改为“按层次与核心的字典序 $(\rho,k)$ 编号”；证明末尾把（C1）的说明提到开头；证明后新增一段：引理只涉及（C1）～（C5），（C6）～（C9）不由构造保证，由评估器在运行时处理 | 这是模型的核心结论，需要在正文完整给出；原稿“全部硬约束自动成立”的说法过强（对应修订对照清单 C1） |
| 引理 4-2（同层独立） | 引理 3-2（同层无跨核边） | 3.6 节 | 结论与一行反证保留在正文；标题由“同层独立”改为“同层无跨核边”，与证明的实际内容一致 | 原标题“独立”容易被理解为同层子图之间没有任何依赖（同核同层的算子仍在一个子图内），改名后更准确 |
| 推论 4-3（单调层指派） | 推论 3-3 | 陈述在 3.6 节；逐点最小性的归纳证明移到附录 D | 正文保留陈述，并说明“两条引理的证明只用到该不等式，因此前半部分直接成立”；附录 D 给出沿拓扑序的归纳证明（写成完整的不等式链） | 前半部分是引理 3-1 证明的直接推论；最小性只用于说明 ALAP 填充与层压缩的合法性，放附录即可 |

## 与证明相关的正文措辞调整

1. 原稿“搜索中无需判环 / 不需要判环”改为“在保持层次单调性的候选生成过程中，无需反复进行商图环检测”，并补充两处实现细节：每次按显式层指派形成子图前，仍用 `levels.check_monotone` 复核一次单调性；第 4 章的模拟退火会打破层次结构，该步骤单独做无环检查。这两处均与代码一致（`solution/npu/levels.py` 的 `check_monotone`，由 `stratify.py` 调用；`solution/npu/p1_anneal.py` 按“子图依赖 ∪ 每核 Task 顺序边无环”判定合法性）。
2. 原稿第 4 章小结“这使后续算法的搜索只需在‘分核’这一维上进行”删除；4.4 节开头改为“CAP-LS 主流程把局部搜索集中在块级核心分配上……候选网格另外覆盖粒度、层指派、子图规模上限等”。
3. 原稿问题二中“第 7 步按层次做稳定排序……引理 4-1 的结论保持不变”保留，编号改为引理 3-1。
4. 原稿问题二“证明跨核搬运量恰为 (λ−1) 超图割”没有给出正式证明。修订稿不再称“证明”，改为“对单生产核中间张量和图输入张量，跨核搬运可写成 (λ−1) 超图割”，并在式下写明求和范围；该结论由评估器的 COPY 插入规则（机制 1）直接读出，并由 `solution/check_traffic_model.py` 在已有方案上逐项核对。
5. 命题前“不考虑通信也是 NP-难”等背景结论沿用原稿对 Ullman、Kwok 与 Karp 的引用，未新增证明。

## 未新增的证明

修订稿没有新增任何定理或证明。原稿中式（4-8）的加速比分解与式（4-7）的理论下界是恒等式与定义性推导，保持原样，只改所在章节。
