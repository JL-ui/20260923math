# 遗留问题清单

本次修订没有改动评估器逻辑，没有重新求解已有配置；正文数字全部由 `paper/v3/tools/std_caliber.py` 从 `results/` 已有官方评估记录计算，唯一的新增计算是单调性修复（第 4 条，已由官方评估脚本逐项复核，非假设）。下面按“需要作者决定 → 本次修订已处理 → 排版与核对 → 已知局限”排列。命令均在仓库根目录运行。

## 一、需要作者决定

### 1. 提交哪一套方案文件【需要作者确认】

修订稿正文与附录 A 的数字对应“标准求解流程”选出的方案，并已包含单调性修复（第 4 条）。仓库现有的 `results/final_plans/` 是离线候选扩展后的方案，含消融、基线、粒度与跨问题复用的方案，平均加速比仍略高（$N=4$：@@OFF_P1_MEAN_4@@、@@OFF_P2_MEAN_4@@、@@OFF_P3_MEAN_4@@ 对标准流程 @@P1_MEAN_4@@、@@P2_MEAN_4@@、@@P3_MEAN_4@@），差距比修订前更小。

- **方案一（推荐）：提交标准流程方案（已含单调性修复）。** 与正文、附录 A 和 `solve.py` 一致，评委可复现。导出与复核命令：
  ```bash
  python paper/v3/tools/build_mono_plans.py --verify --jobs 16   # 若尚未跑过
  python paper/v3/tools/export_std_plans.py          # 写 results/final_plans_std/，不覆盖 final_plans/
  python paper/v3/tools/verify_std_plans.py --problems 2 3 --jobs 16
  python paper/v3/tools/verify_std_plans.py --problems 1 --jobs 16   # 问题一含 4 个大图，较慢
  ```
  说明：导出的 1500 个方案中，1399 个与主实验记录（`results/main.csv`，已在实验时经官方评估器评分）逐位相同；101 个是单调性修复引入的方案，已在本次修订中逐个用官方命令行脚本复核（`results/verify_final_mono.csv`，0 处不一致）。因此不需要对全部 1500 个方案重新跑一遍复核——上面 `verify_std_plans.py` 两条命令仅在你想要一份端到端的、覆盖全部方案的复核记录时才需要运行，不跑也不影响结果的正确性。
- **方案二：继续提交 `results/final_plans/`。** 成绩略高，但提交的方案与正文主结果不是同一口径。此时需要改三处：7.7 节末尾加一句“提交的方案文件取离线候选扩展后的最好结果，其中部分方案来自检验实验，不能由标准流程单独复现”；附录 A 的表注改为离线口径并改用 `results/final.csv` 重新生成；附录 B 的复核命令改回 `solution/verify_final.py`。

### 2. 封面、目录与创新点段落【需要作者确认】

- 封面信息（学校、参赛队号、队员姓名）沿用《完善版》，请核对。
- 《完善版》有目录页，修订稿保留了目录。v2 的格式检查表（F22）曾按格式规范判断“摘要后直接接正文、不设目录”，请按比赛最终的格式要求决定是否保留。
- 按修订要求删除了摘要中独立的“主要创新”段，贡献在摘要第 2 段和第 8 章优点中按内容描述。若格式规范要求单列创新点，可在 8.1 节前加一段，不要加回摘要。

## 二、本次修订已处理（不再需要决定）

### 3. `solve.py` 已改为调用标准流程（L5）

已采纳补丁 `paper/v3/tools/solve_standard_flow.patch`：`solve.py` 默认调用与主实验相同的 `experiment.run_portfolio`，保留 `--grid-only`（旧行为）与 `--fast`。用 `case_001` 问题二 4 核跑通，makespan=58984，与标准流程记录及官方评估脚本直接核对一致。附录 B 已更新，不再有【需要作者确认】段落。

### 4. 标准流程加速比恒不低于 1、随核数单调不降

标准流程的固定候选中加入“整图单核方案”与“本流程自身在更少核数下的方案补一个空核”，每个 (用例, 问题, $N$) 在这些候选与当前网格结果中取 Makespan 最小者。补空核不改变已占用核心的调度、不引入新依赖或新搬运，理论上 Makespan 不变；为避免只依赖这一论断，仍对全部因此改变方案的 @@MONO_FIX_N@@ 个配置逐个用官方评估脚本复核（`results/verify_final_mono.csv`），Makespan 与预期精确相等。工具：`paper/v3/tools/build_mono_plans.py`。

修复后：`case_016` 在标准流程下不再有低于 1 的配置；$\mathrm{MK}_N>\mathrm{MK}_{N-1}$ 的违反次数三个问题均为 0（此前 33、26、21 次）。正文数字（P{1,2,3}\_MEAN\_LIST 等占位符）已随之更新，8.2 节与 8.3 节的相应表述已改写，不再作为“已知缺点”或“未采纳的改进方向”列出。

### 5. 择优只比较 Makespan，与字典序目标式不完全一致（未处理，仍是缺点）

第 3 章的字典序目标式是“Makespan 优先、额外搬运量其次”，但 `run_portfolio` 在 Makespan 相同时取先评估的候选。在 @@LEX_TIE_TOT@@ 个配置中，有 @@LEX_TIE_N@@ 个存在 Makespan 相同但额外搬运量更小的候选。正文已在 8.2 节写明，8.3 节改进方向（1）给出了做法（择优键改为 `(makespan, added_copy_bytes)`），但本次修订没有改代码重跑。

### 6. 问题三在部分配置上慢于问题二（未处理，仍是缺点；数字已随单调性修复更新）

标准流程下问题三有 @@P3SLOW_ALL_N@@/400 个配置慢于问题二（最大 @@P3SLOW_ALL_MAX@@，@@P3SLOW_ALL_MAXC@@），其中同一方案的 FIFO 时序效应只占 @@SAME_SLOW_N@@ 个（最大 @@SAME_SLOW_MAX@@），其余来自两次求解选出的方案不同。正文 6.5.4 节已分两层说明。8.3 节改进方向（1）给出了做法（把问题二的标准流程方案作为问题三的固定候选，跨场景候选复用），需要改代码并重跑问题三主实验，本次修订没有做。

### 7. 未命中来源拆分基于离线方案（未处理，仍是局限）

6.5.3 节“未命中主要来自并发首读与 FIFO 淘汰”取自 `results/l2_sources.json`，该脚本（`solution/l2_sources.py`）对 `results/final.csv` 中的离线方案做完整评估。正文已注明口径。若要改为标准流程方案，需要把脚本的输入换成 `paper/v3/data/final_standard.csv` 后重跑（问题三 400 个配置的完整评估）。

## 三、排版与核对

### 8. Word 在 Linux 上生成，需在 Word 中打开一次

- 修订稿由 `paper/v3/tools/build_v3.py --no-word` 在 Linux 上生成：公式为 pandoc 生成的 OMML，目录页码是按 LibreOffice 排版估计的。
- 打开后请：右键目录 →“更新域”→“更新整个目录”（文件已设置打开时提示更新域）；检查摘要是否在 1～1.5 页内、公式是否正常显示；另存 PDF。
- 在装有 Word 的 Windows 机器上可直接运行 `python paper/v3/tools/build_v3.py`（不加 `--no-word`），由 Word 更新目录并导出 PDF。

### 9. 图中字体

数据图与示意图在 Linux 上用 Liberation Serif（与 Times New Roman 等宽）和 Noto Serif CJK 渲染。若要与正文宋体完全一致，在 Windows 上重跑：

```bash
python paper/v3/tools/figs_diagrams_v3.py
python paper/v3/tools/figs_data_v3.py
python paper/v3/tools/trace_fig_v3.py      # 会调用 3 次评估器命令行，约 1 分钟
```

### 10. 参考文献页码【待核对】

23 条文献沿用 v2 的著录（见 `论文终稿/v2/参考文献核验表.md`）。其中 6 条（Karp、Karypis、Papp 2024、Graham、Kirkpatrick、Topcuoglu）的起止页码在检索页面上核对过；3 条为网络资源（Jia、Papp 2025 SOFSEM、Papp 2025 arXiv），不需要页码；其余 14 条（Liao 的止页、Ullman、Kwok、Çatalyürek、Herrmann、Mirhoseini、Zheng、Gao、Belady、Valiant、Tarjan、Efron、Wilcoxon、Holm）的页码按常见著录填写，定稿前建议在知网或 IEEE/ACM 数据库再核对一次。本次修订没有新增文献。

## 四、已知局限（正文已写明，列此备查）

11. 粒度灵敏度只在 29 个用例、$N=4$ 上做；硬件灵敏度只在 18 个用例、$N=4$ 上做，且 Cache 收益最高的 10 个用例中只有 3 个在样本内。正文已限定结论范围。若有时间，可用 `python solution/run_experiments.py --stage sensitivity --jobs 16 --cases <更多用例>` 扩大样本。
12. 事后消融以单一配置 c2 为基准，问题一只含 96 个用例（4 个大图用例评估太慢未纳入），不代表多候选择优后的边际贡献。
13. 问题二“换入换出减少 64%”的对照基准是 `results/baseline_snapshot/main_before_T11.csv`（加入单算子子图候选之前、只在 c0～c13 中择优的主实验快照）。如需复现该基准，需按快照对应的提交重跑。
14. 第 3 章的执行轨迹图（case_003，问题二，$N=4$）的 (c) 子图改为标准流程方案，数字由评估器命令行重新得到（Makespan 237543 / 202683 / 182260，整核停摆 254527 / 151909 / 92107 cycle），与 `paper/v3/data/trace_stall_v3.json` 一致。该用例未受单调性修复影响（N=4 属于 1399 个“self”配置）。
15. 公式层面发现的两处问题都只改说明、未改公式：超图割式（第 5 章）的求和只对单生产核中间张量与图输入张量成立（已在式下注明）；字典序目标式（第 3 章）与实现的择优规则不一致（见第 5 条）。

## 复核记录

- 单调性修复引入的 101 个方案（占标准流程 1500 个配置的 6.7%）：逐个用官方命令行脚本复核，Makespan 与预期精确相等，0 处不一致。命令：`python paper/v3/tools/build_mono_plans.py --verify --jobs 16`；结果：`results/verify_final_mono.csv`。
- 其余 1399 个方案与 `results/main.csv` 的官方评估记录逐位相同（该记录在主实验运行时已由官方评估器评分并写入哈希缓存），未在本次修订中重新跑官方脚本——重复评估同一方案不会改变其 Makespan，跑与不跑结果一致，故未作为必需步骤（见第 1 条）。
- `solve.py` 补丁：用 `case_001` 问题二 4 核跑通，官方评估脚本核对 makespan=58984，与标准流程记录一致。
