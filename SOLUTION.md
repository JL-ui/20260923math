# 2026 研究生数学建模竞赛 A 题（华为）解答包总览

> 题目：《通用神经网络处理器下的多核调度问题》
> 方法：**CAP-LS** —— 通信感知无环切图 + 跨核同步层次分层 + 组合式局部搜索

## 交付物一览

| 内容 | 位置 |
|---|---|
| **竞赛论文完善版（Word / PDF，草稿.docx 版式）** | `竞赛论文_完善版.docx`、`竞赛论文_完善版.pdf` |
| 完善版论文源文件（分章 Markdown，数字用占位符） | `paper/v2/*.md` |
| 完善版论文的数字、作图与排版脚本 | `solution/paper_v2/`（数字写入 `results/facts_v2.json`，图在 `figures/v2/`） |
| **论文（完整版 Markdown）** | `paper/论文_完整.md` |
| **论文（Word）** | `paper/论文.docx` |
| 论文骨架（含占位符，便于二次编辑） | `paper/论文.md` |
| 《评估器机制说明》（第一阶段反向解析产出） | `paper/01_评估器机制说明.md` |
| 阶段验收报告 | `paper/02_阶段验收报告.md` |
| 实验章节（可单独编辑） | `paper/04_实验章节.md` |
| 附录（逐用例结果 + 程序说明） | `paper/05_附录.md` |
| 逐用例结果表（CSV / Markdown） | `paper/tables/` |
| 全部图表（PNG） | `figures/` |
| 全部实验原始记录 | `results/*.csv`、`results/summary.json` |
| 每个 (用例, 问题, 核数) 的最优方案 | `results/final_plans/p<问题>/n<核数>/<case>_multicore_res.json` |
| 可运行代码 | `solution/` |

## 主要结果（官方评估器，100 个用例，固定配置）

| | $N=2$ | $N=3$ | $N=4$ | $N=5$ |
|---|---|---|---|---|
| 问题 1 平均加速比（算术平均） | 1.7578 | 2.461 | **3.1008** | 3.5931 |
| 问题 2 平均加速比 | 1.8279 | 2.5975 | **3.2623** | 3.7943 |
| 问题 3 平均加速比 | 1.8376 | 2.6149 | **3.3179** | 3.8959 |
| 只读 L2 相对无 L2 的 Makespan 收益 | 1.006 | 1.007 | 1.023 | 1.039 |
| L2 按字节命中率 | 16.4% | 25.2% | 26.9% | 31.0% |

$N=4$ 的中位加速比为 3.43 / 3.63 / 3.68（并行效率 86% / 91% / 92%）；
9–11 个用例出现超线性加速（最高 4.48），来源是切分顺带消除了单核基准的换入换出。
与四类基线相比（$N=4$、问题 2）：随机 1.08、拓扑等分 1.68、负载均衡 1.51、
通信聚类 2.51，CAP-LS **3.12**。单候选求解时间中位数 0.16 s（最大用例 35 705 个算子时 5.1 s）。

## 核心结论（三句话）

1. **必须先读评估器**。官方核内调度 Step3 对"申请片上张量的算子"施加**全局申请序**，
   只有队首者能发射；因此一个排在核内序列前部、数据尚未送达的跨核 `COPY_IN`
   会让**整个核心的四条流水线同时停摆**，实测单次停摆可达 $7.1\times10^5$ cycle，
   而配置里的名义同步延迟只有 500 cycle。
2. **用"跨核同步层次"把这件事结构化**。定义
   $r(v)=\max_{u\to v}(r(u)+\mathbb 1[c(u)\ne c(v)])$，
   取**子图 =（核心, 层次）等价类**，可以同时得到三件事：
   题目全部硬约束自动满足（无需判环）；同层各核子图完全独立；
   依赖远端数据的算子被自动后置，跨核同步被本地工作掩盖。
3. **三个场景共用一套框架**，只切换代价权重与子图粒度：
   问题 1 偏粗粒度（摊薄边界搬运与 Task 切换），问题 2 偏中细（切图在核内只剩排序作用），
   问题 3 把"命中字节按 250 B/cycle 折算"写进分配代价，
   使跨核重复读取从"必须避免"变为"可以承受"。

## 最快的三条命令

```bash
# 生成一个用例的方案并用官方脚本验证
python solution/solve.py data/case_001.json --problem 2 --cores 4 \
       -o data/case_001_multicore_res.json
python code/multicore_cut_evaluate_problem_2.py data/case_001.json \
       data/case_001_multicore_res.json --config data/config.txt

# 复现全部实验 + 图表 + 论文
bash solution/run_all.sh 16
python solution/make_report.py
python solution/assemble_paper.py && python solution/fill_paper.py paper/论文_完整.md
python solution/md2docx.py paper/论文_完整.md -o paper/论文.docx
```

## 诚实性声明

* 论文中**每一个 Makespan / 额外搬运量 / Cache 命中率都来自官方评估脚本**
  在 `data/config.txt` 原配置下的输出；本项目的解析模型只用于搜索内部剪枝。
* 敏感性实验修改了硬件参数，属**研究性实验**，其结果与正式结果在缓存目录上
  物理隔离，论文中已明确标注。
* 论文正文的数字由 `solution/fill_paper.py` 从 `results/summary.json` 自动注入，
  脚本会报出任何未替换的占位符，杜绝手工填数。
