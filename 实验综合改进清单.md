# 实验综合改进清单：让实验撑得起一篇优秀论文

> 2026-09-24。输入：《最终版借鉴评估》《最终版改进清单 v2》两份上传文档，以及仓库 `JL-ui/20260923math` 当前的
> `solution/npu/` 代码、`results/*.csv` 实验记录、`paper/` 现稿。
> 本文所有数字都是这次从 `results/*.csv` 重新算出来的（复算方法见文末附录），不是从两份输入里转抄的。
> 加速比一律按赛题口径（各用例加速比的**算术平均**）。本文只做分析和规划，没有改代码，也没有跑新实验。

---

## 零、一页结论

**总判断：** 算法本身已经有一篇好论文的底子，包括构造性可行性引理、搬运量模型 300 组逐位精确、100 个用例全覆盖、
7 个阶段的实验。离"优秀"差的主要不是再加一个邻域，而是三件事：

1. **报告的成绩不是手里已有的最好成绩。** 仓库里已经经过官方评估的方案中，有 124/400（P1）、124/400（P2）、113/400（P3）
   个配置比最终提交的方案更好。不写新算法、只把已有方案并入择优池，再加上单核兜底和核数单调兜底，
   算术平均加速比就能提高 0.8%～3.5%（P2 N=5：3.794 → 3.928）。
2. **有 8 处论文表述和代码或数据对不上**，其中几处评审一眼就能看出来（例如"自动选择只用 1 个核"、
   "换入换出已被切图消化"、"同时取得更低搬运量"）。这比分数低更伤论文。
3. **实验设计本身有几处硬伤**：基线对比的预算不对等（13 个候选对单配置）；消融做在一个并非最优的单配置上；
   几个子实验的样本是人工挑的，且 L2 敏感性的样本恰好漏掉了全部 L2 高收益用例；
   代理模型检验报的是跨用例混合的秩相关，这个指标掩盖了用例内部的排序能力；全文没有置信区间和显著性检验。

**真正的提分杠杆**只有一个经过外部验证的：场景 B 下"每算子一子图 + 容量感知表调度"（同学论文的做法，
在 5 个共同用例的全部 60 个配置上不劣于 CAP-LS）。其余算法改进项都应排在它和保底项之后。

**执行顺序（前 12 项）：**

| 序 | 项 | 类型 | 成本 | 预期收益 |
|---|---|---|---|---|
| 1 | A1 口径改算术平均 + A2 修 8 处不一致 | 论文 | 0.5 天，不跑实验 | 消除硬伤 |
| 2 | B1–B5 保底池（已知最优池、跨问题、跨核数、单核、P2/P3 大图不裁剪） | 算法小改 | 0.5–1 天，P2/P3 重跑约 5 CPU·h | 算术平均 +0.8%～3.5%；min≥1、N 单调、P3≥P2 全部成立 |
| 3 | D3 统计规范（CI、配对检验、胜平负） | 分析脚本 | 0.5 天，不跑实验 | 所有比较结论有统计支撑 |
| 4 | C3 逐字节等价的快速评估器副本 | 工程 | 1 天 | P1 端到端 6329 s → 预计 <10 min，解锁 P1 的一切后续实验 |
| 5 | C1 场景 B 单算子子图 + 容量感知表调度候选 | 核心算法 | 2–3 天 | 5 个共同用例上 P2 N=4 差距约 0.7（3.27 vs 3.98） |
| 6 | C2 统一时间模型（同时用作 C1 调度器、P2/P3 代理、P3 FIFO 时间序） | 核心算法 | 1–2 天，离线验证零评估 | 代理用例内秩相关从 0.48 提升；局部搜索从 0 贡献变正 |
| 7 | D1 基线体系重建（预算对等 + 经典外部基线） | 实验 | 1 天，约 10 CPU·h | 对比结论可信 |
| 8 | D2 消融体系重建（机制 / 组合 / 择优三层） | 实验 | 1 天，大部分用已有数据 | 每个模块的贡献可解释 |
| 9 | D4 + D8 样本规范化 + P3 的 L2 专项实验 | 实验 | 1 天，约 5 CPU·h | P3 这一问的核心结论站得住 |
| 10 | D6 界与参照（逐用例界、已知最优参照） | 分析 | 0.5–1 天 | 回答"离最优还有多远" |
| 11 | C4 冠军邻域官方精修 + C5 P1 专属杠杆 | 算法 | 2–3 天 | 视 C2 结果 |
| 12 | D5 过拟合检验、D7 可复现、E 图表 | 实验与写作 | 1 天 | 论文完整度 |

时间只够做一部分时，按"1 → 2 → 3 → 5 → 7/8 的已有数据部分"的顺序做，保证论文不出错、成绩不白丢、
有一个真正的新杠杆、关键比较有统计支撑。

---

## 一、按评审视角的现状体检

| 维度 | 现状（本次复算） | 评审会怎么问 | 风险 |
|---|---|---|---|
| **结果口径** | 正文、图 5/10/11、表 10-1、SOLUTION.md 都用几何平均（`04_实验章节.md:22`）；赛题要求算术平均 | "你的平均加速比和赛题定义不一样" | 高 |
| **结果是否已尽力** | 已评估方案里有更优解：P1 124/400、P2 124/400、P3 113/400 个配置，平均多 2.2%，最坏 90.6%（case_068 P2 N=5） | "你手里有更好的解为什么不交" | 高 |
| **稳定性** | 19/19/14 个用例（P1/P2/P3）存在 N+1 核比 N 核慢 1% 以上，最坏 1.39 倍；最小加速比 0.9985 < 1；P3 劣于 P2 的配置有 2–7 个（最坏 0.86） | 赛题原文要求"在 2～5 核范围内均能稳定地……生成高质量结果" | 高 |
| **基线** | 4 个基线全是自己构造的"去掉某个机制"的变体，全部不分层、单配置；CAP-LS 是 13 候选 + 官方评估择优 | "预算不对等；没有经典算法（HEFT、多级图划分）；没有外部参照" | 高 |
| **消融** | 在单配置 c0（LPT 起点）上做；但 c0 并不是最强的单配置，c2（轮转起点）更强；组合择优本身没有消融 | "你消融的是论文里的算法，还是实际交上去的算法？" | 中高 |
| **统计** | 没有置信区间、没有显著性检验、单一随机种子 | "+0.7% 的差异是不是噪声" | 中 |
| **样本** | 代理检验只用 case_001–040 且只有 N=4（`validate_model.py:190`）；粒度实验 25 个、敏感性 18 个，都是手挑的；敏感性样本里 L2 收益最大只有 1.066，而全体中有 8 个用例 >1.05，最大 1.726 | "为什么是这几个用例"，并可能被认为是选择性取样 | 高（P3） |
| **界与参照** | 只有 LB=max(M/N, V/N, B/bw, CP) 的几何平均比值；单核方案自己就是 LB 的 1.15 倍（中位），所以比值里混着不可消除的部分 | "离最优到底多远，哪部分能消除" | 中 |
| **时间** | P1 有 30 个配置、12 个用例端到端超过 600 s，case_014 达 5477–6329 s；P2/P3 最长 92–95 s | 赛题要求"在合理时间内……高效地生成" | 中高 |
| **可复现** | 有一键脚本和评估缓存；没有 `requirements.txt`，运行环境是 Windows 11；没有"对最终方案逐一跑官方 CLI 并比对附录"的校验脚本 | 附件"提供可复现的 Python 程序脚本" | 中 |
| **论文与代码一致** | 8 处不一致（见 §2.8） | 直接影响可信度 | 高 |

---

## 二、本次核实中新发现的问题

以下问题在两份输入中没有提到，或者提到了但数字、结论需要更正。

### 2.1 最终成绩没有用上已经评估过的更好方案

把 `main / baseline / ablation / granularity / p3_compare` 五个 CSV 里所有在官方固定配置下评估过的方案按
（用例, 问题, N）取最小 Makespan，作为"已知最优"，再和 `main.csv` 的冠军对比：

| | 冠军劣于已知最优（>0.1%）的配置数 | 平均多出 | 最坏 |
|---|---|---|---|
| P1 | 124 / 400 | 2.24% | 74.5%（case_068 N=5） |
| P2 | 124 / 400 | 2.23% | 90.6%（case_068 N=5） |
| P3 | 113 / 400 | 1.57% | 48.7%（case_068 N=5） |

这些方案来自同一套代码的确定性重建（消融、粒度、基线的各个变体），全部经过官方评估器、全部在正式配置下，
可以合法地作为提交方案。再加上"N−1 核冠军可以作为 N 核方案"（空一个核，`cores_used` 候选已经证明评估器接受这种方案）
和"单核方案兜底"，算术平均加速比的变化是：

| | N=2 | N=3 | N=4 | N=5 |
|---|---|---|---|---|
| P1 当前 → 保底池 | 1.758 → 1.774 | 2.461 → 2.493 | 3.101 → 3.154 | 3.593 → 3.713 |
| P2 当前 → 保底池 | 1.828 → 1.842 | 2.598 → 2.629 | 3.262 → 3.326 | 3.794 → 3.928 |
| P3 当前 → 保底池 | 1.838 → 1.853 | 2.615 → 2.644 | 3.318 → 3.361 | 3.896 → 3.999 |

P3 一行用"P2 方案在 P3 下的 Makespan ≤ 它在 P2 下的 Makespan"做了保守替代（现有数据中同一方案开 L2 后从未变慢，
`p3_compare.csv` 各 N 最小比值 1.000），所以 P3 的真实提升只会更大。这是整份清单里**性价比最高的一项**。

### 2.2 局部搜索在三个问题上用的是同一个粗代理，P1 的事件模拟从未进入搜索

`algorithms._local_search`（`algorithms.py:225`）对三个问题都调用 `TrafficState.cost`（`algorithms.py:138`），
也就是 max(负载, DDR) + λ·同步深度。`assign.estimate_scene_a`（`assign.py:112`）和 `estimate_scene_b`（`assign.py:171`）
**在整个求解流程中没有任何调用方**，只有 `validate_model.py` 用到（而且它用的是自己文件里的 `estimate_scene_a_from_plan`）。

所以两份输入都有的一个判断需要更正："P1 代理很准（0.992），所以 P1 局部搜索有用、P1 不需要改代理"。
实际上 P1 被检验的代理和 P1 搜索用的代理是**两个不同的函数**。P1 消融里去掉局部搜索的变化是 +0.3%（N=4 算术平均
2.890 vs 2.880，全 N 配对 Wilcoxon p=0.105，不显著），和 P2/P3 一样没有贡献。论文 §10.7 的"$F$ 足以驱动局部搜索"
（`04_实验章节.md:237`）因此同时被代码和消融否定。

### 2.3 "Spearman 0.97～0.99"是跨用例混合的秩相关，掩盖了用例内部的排序能力

论文和两份输入引用的 0.992 / 0.969 / 0.971 是把 40 个用例 × 12 个候选的 480 个点混在一起算的。
不同用例的 Makespan 相差几个数量级，混合秩相关主要反映的是"大图比小图慢"。择优时真正要比的是**同一用例内**候选之间的排序：

| | 用例内 Spearman（中位 / 均值） | 代理选择损失（中位 / 均值 / 最大） | Recall@1 | Recall@3 | Recall@5 |
|---|---|---|---|---|---|
| P1 | 0.95 / 0.90 | 0.0% / 1.5% / 16.5% | 72% | 78% | 92% |
| P2 | **0.48** / 0.51 | 2.4% / 15.1% / **158%** | 40% | 48% | 55% |
| P3 | **0.44** / 0.51 | 2.6% / 15.7% / **159%** | 38% | 52% | 55% |

（Recall 与 v2 的数字差 1 个样本左右，来自并列候选的处理方式。）两点结论：

* "均值损失 15%"是被个别用例的 150% 级长尾拉起来的，中位数只有 2.4%。论文应该同时报中位数、90 分位和最大值，
  不能只报均值，更不能只报混合秩相关。
* P2/P3 代理在用例内的排序能力接近随机（中位 0.48），这才是局部搜索没有贡献的直接证据，比"均值损失 15%"更有说服力。

### 2.4 最大的图反而得到最少的搜索

`run_portfolio`（`experiment.py:97`）在 >12000 算子时只用 4 个候选（`level='fast'`），在 >6000 算子时只用 8 个。
100 个用例中有 17 个只跑 4 个候选、12 个只跑 8 个。裁剪的理由是 P1 评估器太慢，但它对 P2/P3 同样生效，
而 P2/P3 端到端最长只有 92–95 s。case_016（17,995 个算子）就是因此没有 `cores_used` 候选的。

### 2.5 候选组合的边际价值很不均匀

对 71 个跑满 12 个候选的用例（N=4）做贪心前向选择：

| | 1 个候选 | 4 个候选 | 12 个候选 |
|---|---|---|---|
| P1 | c2（轮转）2.786 | +c6、c3、c10 → 2.951 | 2.975 |
| P2 | c2（轮转）2.943 | +c7、c6、c10 → 3.115 | 3.174 |

作为"主算法"的 c0（LPT + 局部搜索）在 P2 贪心序列里排最后，边际贡献为 0；最强的单配置是 c2（轮转起点）。
这和消融中 `no_balance` 更好（P2 N=4 算术平均 3.092 vs 2.996，全 N 配对 p=0.0006）是一致的。
论文把 LPT + 局部搜索写成核心机制，但数据显示它们既不是最强单配置、也不是组合里有贡献的成员。

### 2.6 基线对比的差距有一大半来自预算差

| N=4 算术平均 | P1 | P2 | P3 |
|---|---|---|---|
| 最强基线 B4（通信聚类，单配置） | 2.348 | 2.784 | 2.830 |
| CAP-LS 单配置 c0 | 2.880（+22.7%） | 2.996（+7.6%） | 3.072（+8.6%） |
| CAP-LS 组合（13 候选 + 官方择优） | 3.101（+32.1%） | 3.262（+17.2%） | 3.318（+17.2%） |

论文写的"相对最强基线 B4 仍有 24%–42% 的提升"（几何平均、组合对单配置）里，P2/P3 超过一半来自"多评估几次取最好"，
而不是算法机制。另外 CAP-LS 组合在 15/34/32 个配置（P1/P2/P3，共 400 个）上输给至少一个基线。
`no_level` 消融（P2 N=4 2.777）和 B4（2.784）几乎相等，说明现有基线本质上就是"不分层的 CAP-LS"，
它们证明的是"分层有用"，不能证明"CAP-LS 优于现有方法"。

### 2.7 换入换出是额外搬运的主体，论文却说它"已被切图消化"

| 最优方案 | spill 占额外搬运的比例 | 40 个 spill 用例的 spill 是单核的 | 完全消除 spill 的用例 |
|---|---|---|---|
| P1 N=2 / 4 / 5 | 85% / 70% / 68% | 90% / 87% / 85% | 12 / 10 / 9 |
| P2 N=2 / 4 / 5 | 90% / 74% / 69% | 89% / 83% / 81% | 9 / 13 / 9 |

N=4 P2 的 9 个超线性用例全部落在这 40 个 spill 用例里。论文 §11.3 第 2 条"换入换出已被切图本身消化掉了"
（`04_实验章节.md:380`）与此矛盾。同学的方案 75 个全部零 spill。spill 同时决定了次要指标（额外搬运量）的大头，
所以减少 spill 能同时改善主次两个指标。

### 2.8 论文与代码或数据不一致的 8 处

| # | 位置 | 论文写法 | 实际情况 |
|---|---|---|---|
| 1 | §10.1（`04_实验章节.md:22`）及全文 | 跨用例用几何平均 | 赛题定义是算术平均；算术值已在 `summary.json` 的 `speedup_mean` |
| 2 | §7.2 P2-C（`论文.md:382`） | "算子数 × 平均张量大小近似峰值驻留" | 代码里只有 `stratify.py` 的 `max_ops` 按算子数切，没有驻留估计 |
| 3 | §8.2 | P3 下"θ 可以取得更小" | θ 的候选序列 `(INF,16,8,4,2,1)` 与问题无关（`partition.py:282`），P3 没有更小的 θ |
| 4 | §8.3 | 复用度、Cache 价值与 FIFO 估计"由 `npu/cachemodel.py` 计算……用于与官方实测命中率对照" | `cachemodel.py` 在全仓库没有调用方，也没有任何对照实验的输出 |
| 5 | §10.3（`04_实验章节.md:112`） | case_016 上组合择优"自动选择只用 1 个核"，保证加速比不低于 1.00 | case_016 只跑了 4 个候选、没有 `cores_used` 候选；三个问题全部 N 的冠军都是 c3（连续段），用满 N 个子图，加速比 0.9992–0.9999，低于 1 |
| 6 | §10.4（`04_实验章节.md:153`） | Pareto 图上 CAP-LS "同时取得更高加速比与更低额外搬运量" | P1 N=4 CAP-LS 额外搬运总量 1.53×10⁹，高于 B2/B3 的 1.48×10⁹；P2 为 1.39×10⁹ 对 1.42–1.44×10⁹，中位数 1.05×10⁶ 对 B3 的 1.01×10⁶，只能说"持平" |
| 7 | §10.2 第 1 条（`04_实验章节.md:57`） | "加速比随核数单调上升" | 只对均值成立；逐用例有 19/19/14 个用例不单调 |
| 8 | §11.3 第 2、4 条（`04_实验章节.md:380, 392`） | spill "已被切图消化"；"若不做分层……对延迟的敏感度会显著上升" | spill 见 §2.7；仓库里没有任何 `no_level` 的敏感性实验 |

另有两处表述应弱化：§10.7 的"$F$ 足以驱动局部搜索"（见 §2.2）；§10.7 下界段的"剩余差距主要来自跨核同步与负载不可整除性，
而不是来自切图质量"没有实验支撑（见 D6）。

### 2.9 敏感性实验的"默认点"不是正式结果

`task_sensitivity` 用 `candidate_params(..., 'fast')`（`run_experiments.py:107`），只有 4 个候选。
默认配置点的 Makespan 平均比正式结果差 4.3%，最坏差 28.8%。相对倍数本身是自洽的，但论文需要说明这个口径，
或者改用和正式结果相同的候选集。

---

## 三、两份输入的分歧与裁定

| # | 分歧 | 《借鉴评估》 | 《清单 v2》 | 裁定与依据 |
|---|---|---|---|---|
| D1 | **第一优先是什么** | 场景 B 事件模拟代理第一，单算子子图第五 | 保底之后先做单算子子图（2-0），代理验证排第 6 | **单算子子图优先，代理并行做离线验证。** 理由：(1) 最终择优已经由官方评估器完成，代理误差只影响局部搜索和候选剪枝，而局部搜索的贡献上限本身不确定；(2) 单算子子图是唯一有外部实测的杠杆（5 用例 60 配置全部不劣）；(3) 它作为新候选交官方评估，**不依赖代理**。但 C1 的表调度本身需要一个时间模型，所以最好把两件事做成同一个引擎（C2），一次开发三处复用 |
| D2 | **P1 要不要动代理** | P1 代理很准，只做 Top-K 控时 | 同左 | **两份都基于错误前提**（§2.2）。P1 搜索从未用过事件模拟。P1 不需要为提分改代理，但如果做 P1 的冠军精修，应该把 `estimate_scene_a_from_plan` 接进去，而不是继续用 `TrafficState.cost` |
| D3 | **P1 控时怎么做** | 代理 Top-3 交官方评估 | 按代理顺序依次评估 + 时间预算；另设快速评估器副本，但副本"只跑 step1–2、step3 只估时，Spearman>0.95 即接入" | **两种都不如逐字节等价的评估器副本。** Top-K 有召回风险（P1 R@3 78%，最坏损失 16.5%）；v2 的近似副本本质上又是一个代理，同样有排序误差。同学的做法是复制评估器、把两处 O(n) 扫描换成计数器，约 15 倍提速且**逐字节一致**。它的验证可以用仓库现有 1 万多条官方评估缓存做**完全相等**检验，零质量风险。做成后 P1 不需要剪候选（C3） |
| D4 | **B3（σ 上的切口 DP）** | 列为"适合写成论文创新点" | 适用面窄，降级 | **v2 对。** 复算：β=0.35、N=4 时 12/100 个用例存在超限原子（β=0.15 时 32 个）；这 12 个用例的 P2 N=4 平均加速比 3.23，与其余用例的 3.27 无差别；29 个最难用例（ρmax≥0.5）中只有 3 个在其中。它不是瓶颈，放入展望 |
| D5 | **(λ−1) 超图建模** | "理论与赛题定义完全对应，论文说服力强"，列为远期算法项 | 修正了公式：场景 B 中间张量 2s(λ−1)，图输入 s(λ−1)，场景 A 多一次 COPY_OUT | **v2 的公式对，而且这个量已经被 `TrafficState._apply` 逐项精确计算**（每个"生产核, 消费核"对插一对 COPY，搬运量模型 300 组逐位一致）。所以作为建模它是**写作项**：把 §5.4 的搬运项改写成带权连通度 (λ−1) 目标，接上超图划分文献即可，不需要写代码。作为算法（换成超图划分器）优先级低：边界搬运只占 N=4 P2 额外搬运的 26%，DDR 项只在 1/100 个用例上是下界的决定项，带宽加到 4 倍也只快 6.4% |
| D6 | **P2/P3 代理有多差** | "均值损失 15%" | 同左，加 Recall@K | 两份都用了均值和混合秩相关。**应改用用例内秩相关（中位 0.48）和损失分布（中位 2.4%，最大 158%）**，见 §2.3 |
| D7 | **单算子子图的验收标准** | 5 个用例对比同学 | "5 个公共用例差距缩小 50%" | **不能只用 5 个用例验收**，否则是在同学的测试集上调参。验收应该是 100 个用例上 P2/P3 算术平均不退步、并报告按 ρmax 分层的收益；5 个用例只作为外部参照 |
| D8 | **"单调不退化"是否已成立** | "HJR 声称的单调不退化现在已经成立" | 提出 n_act∈{N, N−1} 保障 | **没有成立。** 它只在单个（用例, 问题, N）的候选池内成立；跨核数（19 个用例不单调）、跨问题（P3<P2）、跨实验阶段（§2.1 的 124/400）都不成立 |
| D9 | **P1 相邻 Task 合并（v2 1-4）是否值得** | 未列 | 列为保底项 | **值得保留为候选。** P1 最优方案中有 167/400 个配置的子图数多于核数，合并有适用面；成本是每组多 1 次评估，但 P1 评估贵，建议放在 C3 之后 |
| D10 | **首读错峰（v2 3-4）** | 第八优先的一部分 | 独立一项，依赖 2-0 | 同意依赖 C1。但 L2 在同一方案上的平均收益只有 1.02–1.04，这一项的价值主要在 P3 的论述，不在分数，排在 D8 之后 |

---

## 四、综合改进清单

每一项注明：做什么、依据、对论文哪一部分有帮助、成本、验收标准、依赖。
优先级：**P0** 必做（不做论文有硬伤或白丢分）；**P1** 核心提分或核心论证；**P2** 让论文从"良好"到"优秀"；**P3** 展望。

### 第 0 层　论文可信度修正（P0，不跑实验）

**A1　平均加速比改为算术平均**　｜ P0 ｜ 0.5 天
- 做什么：`plots.py` 的 fig05/05b/10/11/17 纵轴改为算术平均；正文和表 10-1、10-3、11-1、11-2、11-3 改引 `speedup_mean`
  （走 `@@KEY@@` 占位符）；几何平均和中位数保留为补充列；§10.1 的统计口径段落改写，理由写"遵守赛题定义"。
- 依据：赛题"平均加速比折线图"；`summary.json` 已有 `speedup_mean`（P2：1.828/2.598/3.262/3.794）。
- 论文受益：§10.1、§10.2、摘要、结论。
- 验收：`fill_paper.py` 无未替换占位符；全文搜索"几何平均"只出现在标明"补充指标"的位置。
- 依赖：无。等 B 层完成后数字会再变一次，所以占位符一定要走自动注入。

**A2　修正 §2.8 列出的 8 处不一致**　｜ P0 ｜ 0.5 天
- 做什么：逐条改写或补实验。建议处理方式：
  #2 P2-C 改为"用 `max_ops` 候选间接抑制超大子图"，C7 做成后再改回；
  #3 θ 删掉"P3 取更小"，或在 D8 里补 θ 扫描后再写；
  #4 `cachemodel.py` 二选一：接入 D8 做"预测命中率 vs 官方命中率"散点图，或者删掉 §8.3 的那句；
  #5 case_016 改为如实描述，B4 做完后可以写"单核兜底保证不低于 1"；
  #6 Pareto 改为"加速比显著更高、额外搬运量与基线持平"；
  #7 改为"平均加速比随核数单调上升"，B3 做完后可以写"逐用例单调"；
  #8 删掉 `no_level` 敏感度那句，spill 那句改成与 §2.7 一致的数据。
- 依据：§2.8 表中的代码位置与数据。
- 论文受益：§7.2、§8、§10.2–10.4、§11.3。
- 验收：每一句定量或机制性的断言都能在附录的"论断—证据对照表"（E2）里找到对应的数据或代码位置。
- 依赖：#3/#4/#5/#7 的最终写法依赖 B 层和 D8。

**A3　代理模型检验改用正确的指标**　｜ P0 ｜ 0.5 天（只用已有数据）
- 做什么：§10.7 表格改为用例内 Spearman/Kendall τ 的分布、Recall@1/3/5、选择损失的中位数/90 分位/最大值；
  写明 P1 搜索用的代理与被检验的事件模拟不是同一个函数（或在 C2 之后让它们一致）；删除"足以驱动局部搜索"。
- 依据：§2.2、§2.3。
- 论文受益：§10.7，并且给 C2 的必要性提供一个诚实的动机。
- 验收：表中没有跨用例混合的相关系数（可以放在脚注）。
- 依赖：无。C2 做完后在同一张表里加一行"新代理"。

**A4　基线对比与消融改用统一口径**　｜ P0 ｜ 随 D3 一起做
- 做什么：主口径定为 N=4 的算术平均，另附全 N 合并；§10.4 的"24%–42%"改为区分"单配置对单配置"和"组合对单配置"两个数。
- 依据：§2.6。
- 论文受益：§10.4、§11.1。

### 第 1 层　零成本保底（P0，只用已有评估或极少新评估）

这一层 5 项合起来只改 `experiment.run_portfolio` 和结果汇总，不碰四条不变量。

**B1　已知最优池**　｜ P0 ｜ 0.5 天
- 做什么：最终择优时，把同一（用例, 问题, N）在正式配置下评估过的**所有**方案都纳入：基线、消融变体、粒度扫描、P3 交叉评估。
  实现上最简单的是让 `run_portfolio` 把这些变体的参数也作为候选（它们都是 `cap_ls` 或基线函数的确定性输出，评估结果已在
  `results/cache/p{1,2,3}` 中，命中缓存不需要重新评估）。敏感性阶段的方案不能纳入（配置不同）。
- 依据：§2.1，124/124/113 个配置可改进。
- 论文受益：§10.2 主结果直接提高；同时 §10.4 的"CAP-LS 输给某个基线"的 15/34/32 个配置自动消失。
- 验收：最终冠军 = 已知最优（全部 1200 个配置）。
- 依赖：无。注意论文里要如实说明"组合候选包含基线与消融变体"，这对算法描述是加分（多起点更丰富），不是减分。

**B2　跨问题互投**　｜ P0 ｜ 随 B1
- 做什么：P2 冠军进 P3 池、P3 候选进 P2 池（用 P2 评估器）。
- 依据：P3 劣于 P2 的配置 2/2/7/6 个（N=2..5），最坏 0.86（case_010 N=5）；P3 方案在 P2 评估器下比 P2 冠军好 >0.1% 的有 28 个配置，最大 1.38 倍（case_068 N=5）。
- 论文受益：§10.5 可以写"P3 ≥ P2 对全部用例成立"。
- 验收：每个 N，P3 ≥ P2 的用例数 = 100。
- 依赖：无。P3 池每配置多一次评估。

**B3　核数单调兜底**　｜ P0 ｜ 随 B1
- 做什么：N 核的候选池加入 N−1 核的冠军（第 N 个核的调度表留空）。按 N=2→5 顺序跑，天然可以复用。
- 依据：不单调的用例 19/19/14 个，最坏 1.39 倍（case_068 P1 N=5、case_044 P2 N=4）。同学用 n_act∈{N, N−1} 做到了 75 个方案无一退步。
- 论文受益：§10.2 可以写"逐用例单调"，直接回应赛题的"稳定地"。
- 验收：所有用例、所有问题，Makespan(N) ≤ Makespan(N−1)。
- 依赖：无。

**B4　单核兜底 + 搬运量次关键字**　｜ P0 ｜ 随 B1
- 做什么：候选池加入"整图放 0 号核"方案（官方结果等于单核基准，论文已验证 100/100 一致）；择优键改为（Makespan, 额外搬运量）。
- 依据：最小加速比 0.9985–0.9996；赛题"以 Makespan 为主，兼顾总额外数据搬运量"；`run_portfolio` 只比 Makespan（`experiment.py:117`）。
- 论文受益：§10.3 case_016 的描述变成真的；附录没有小于 1 的加速比。
- 验收：1200 个配置的加速比全部 ≥ 1.000。

**B5　P2/P3 取消大图候选裁剪**　｜ P0 ｜ 0.5 天，重跑约 5 CPU·h
- 做什么：`run_portfolio` 的 `level='auto'` 与 `grid[:8]` 只对 P1 生效；P2/P3 一律跑满候选。
- 依据：§2.4，29 个大图用例被裁剪；P2/P3 端到端最长 92–95 s，main 阶段 P2、P3 各只用了 2.3 CPU·h。
- 论文受益：§10.3 大图难例；§10.6 可以写"P2/P3 对全部用例使用同一候选集"。
- 验收：P2/P3 端到端最长仍在 10 分钟内；大图用例的加速比不降。
- 依赖：无。P1 的同类改动等 C3。

> **B 层合计**：按现有数据可直接算出的提升为 +0.8%～+3.5%（§2.1 表），B5 的额外收益需要重跑后才知道。
> 做完 B 层应立即重跑 `finalize.sh`，把论文里所有数字更新一遍。

### 第 2 层　核心提分（P1）

**C1　场景 B：每算子一子图 + 容量感知表调度（新候选）**　｜ P1 ｜ 2–3 天
- 做什么：在现有分核结果 c(v) 上，每个算子单独成子图，核内顺序由一个表调度生成：
  M/V 两条流水分开建模；跨核前驱的就绪时间 = 远端完成 + 500 + 字节/带宽；可发射条件加驻留上限 α·C（α 取 0.8 起扫）；
  排序第一关键字仍是同步层次 r(v)，保证不变量 2（每算子子图是 (核, 层) 等价类的细化，按 (r, 表调度位置) 编号后商图仍无环）。
  作为新候选加入 P2/P3 池，由官方评估择优。
- 依据：官方 `_prioritize_task_seq`（`code/multicore_cut_evaluate_problem_2.py:43`）对子图优先级做稳定排序，每算子一子图时核内全序完全由方案决定；
  同学在 5 个共同用例上 P2 算术平均 2.26/3.26/3.98/4.75，CAP-LS 1.71/2.57/3.27/3.74；case_003 P2 N=2：237,185 对 552,700；
  同学 75 个方案零 spill，而本项目 spill 占额外搬运的 69%–90%（§2.7）。
- 论文受益：§7（P2 算法）新增一个机制；§10.2 主结果；§10.3 难例；同时是 spill 问题的主要解法（§2.7），额外搬运量也会下降。
- 成本：中。风险在于 3.6 万算子的图上每算子一子图可能让评估器变慢，**先在分层抽样的 20 个用例上试点**（每个 ρmax 层都有，含最大的 3 个图），测评估时间。
- 验收：100 个用例上 P2/P3 算术平均不退步（它是新增候选，理论上不会退步）；报告该候选胜出的用例数、按 ρmax 分层的收益、spill 字节的变化；
  5 个共同用例与同学结果并列作为外部参照。
- 依赖：C2 的时间模型（可以先用简化版：只有 M/V 流水和就绪时间）。

**C2　统一时间模型（一个引擎，三处复用）**　｜ P1 ｜ 1–2 天，离线验证零评估
- 做什么：写一个以（核, 层）子图或单算子为单位的事件模拟：开始时刻 = max(同核上一段结束, 远端前驱结束 + 500 + 跨核字节/带宽)，
  时长 = max(M, V, MTE2, MTE3)，全局再与 DDR 下界取 max。用在三个地方：(a) C1 的表调度；(b) P2/P3 候选排序和局部搜索的代价
  （替换 `TrafficState.cost` 里的 λ·深度项）；(c) P3 的 FIFO 命中模拟改用它给出的时间序（`cachemodel.py:93` 现在按张量 id 排序）。
  两份输入里的 StallRisk、加权同步深度 τ 都是它的分量，不需要单独做。
- 依据：§2.2、§2.3；P2/P3 用例内秩相关中位 0.48；P1 的 Task 级事件模拟用例内中位 0.95，差别就在"是否模拟等待"。
- 论文受益：§7.1 模型、§10.7 代理检验（新旧对比一张表）、§11.1 消融（局部搜索贡献由零变正的话就是一条正面结论）。
- 验证：**先离线**。把 `validate_model.py` 的样本扩大到全部 100 个用例 × N=2..5（它会确定性重建候选并命中缓存，不需要新评估），
  比较新旧代理的用例内秩相关、Recall@K、损失分布。
- 验收：P2/P3 用例内 Spearman 中位数明显高于 0.48，Recall@3 明显高于 50%；达不到就只用于 C1、不接进局部搜索。
- 依赖：无。与 C1 并行开发。

**C3　逐字节等价的快速评估器副本**　｜ P1 ｜ 1 天
- 做什么：把 `code/` 下评估链复制到 `solution/npu/`（**不改 `code/`**），只做纯性能优化（例如把 O(n) 的就绪扫描、申请序队首检查换成计数器和堆），
  不改任何语义。只用于候选择优，所有写进论文的数字仍来自官方评估器。
- 依据：P1 有 30 个配置端到端 >600 s，case_014 达 5477–6329 s，main 阶段 P1 用了 35.6 CPU·h（P2/P3 各约 2.3）；
  单次官方评估最长 1829 s；同学报告副本约 15 倍提速且逐字节一致。
- 验收：对缓存中的全部方案（去重后 10,780 次官方评估，三个问题都有）逐字段**完全相等**；P1 全部用例端到端 < 10 分钟。
- 论文受益：§10.6 可以写"全部用例在 X 分钟内完成"；更重要的是它让 P1 也能做 B5、C4、D1–D5 的实验，否则 P1 的每次重跑都要一天以上。
- 依赖：无。如果做不到逐字节一致，退回 v2 的"按代理顺序评估 + 时间预算 + 必评安全候选"方案，并在论文里报告与全量评估相比的损失。

**C4　冠军邻域的官方评估精修**　｜ P1 ｜ 1–2 天
- 做什么：以组合冠军为起点并永久保留，生成 Move / Swap / 按算子搬动分核边界 的邻居，重新分层（`level` 模式对任意算子到核的映射重算 r(v) 即合法），
  用 C2 排序后只把 Top-K 交官方评估；更好（或同 Makespan 更少搬运）才替换。P2/P3 先做，P1 在 C3 之后做。
- 依据：两份输入一致认可的方向；P2/P3 单配置评估中位不到 1 s，每配置追加 20–40 次评估仍在 10 分钟内。
- 论文受益：§6.3/§7.2 的"局部搜索"从论文里一个没有贡献的模块，变成一个有实测增益的模块。
- 验收：精修后优于组合冠军的配置比例；新增评估次数与时间。
- 依赖：C2（排序准确度决定 Top-K 的命中率）。

**C5　P1 专属杠杆：相邻 Task 合并与层级区间 DP**　｜ P1 ｜ 合并 0.5 天，DP 2 天
- 做什么：(a) 同核相邻、之间没有跨核边的 Task 合并，作为新候选；(b) 同学论文的 PSD：按 ASAP 层把 DAG 切成阶段，
  阶段内弱连通分量为流，用 F_{b+1} = min_a (F_a + Φ(a,b)) 在阶段区间上选切点，Φ 用 `estimate_scene_a_from_plan`。
- 依据：P1 最优方案中 167/400 个配置子图数多于核数（合并有适用面）；同学 P1 在 5 个用例上 1.90/2.80/3.45/4.18，CAP-LS 1.71/2.36/2.91/3.28。
- 论文受益：§6 P1 算法。
- 验收：100 个用例上 P1 算术平均不退步；报告新候选的胜出数。
- 依赖：C3（否则 P1 的每轮验证都要一天以上）。

**C6　候选集重组**　｜ P1 ｜ 0.5 天，用已有数据
- 做什么：用贪心前向选择（§2.5）确定候选的边际价值，把 C1、C5 等新候选加进去后再做一次；论文里给出"候选数—平均加速比"曲线，
  并把主算法描述从"LPT + 局部搜索"改为与数据一致的"亲和原子化 + 同步层次分层 + 多起点组合"。
- 依据：P2 中 c0 边际贡献为 0，4 个候选即达 12 个候选的 98%。
- 论文受益：§6.3 算法描述；§10.6 可以论证"候选数与时间的权衡"。
- 依赖：B 层、C1。

**C7　内存感知的子图拆分（非单算子候选上减少 spill）**　｜ P2 ｜ 2 天
- 做什么：在每个（核, 层）桶内沿 σ 扫描活跃字节（birth→lastuse），超过 L1/UB 容量处拆成拓扑一致的子段；与 C1 共用驻留估计。
- 依据：§2.7；40 个 spill 用例；超线性用例全部在其中。
- 验收：40 个用例上 spill 字节显著下降且 Makespan 不退化；做成后把 A2 #2 改回"驻留估计"。
- 依赖：C1（如果 C1 已经把大部分 spill 消除，这一项降为 P3）。

### 第 3 层　实验体系补强（P1–P2，让结论站得住）

**D1　基线体系重建**　｜ P1 ｜ 1 天，约 10 CPU·h（P1 部分等 C3）
- 做什么：
  1. **预算对等**：每个基线也给同样的评估预算（例如基线 × β∈{0.08, 0.15, 0.35, 0.6} × 3 个种子，取最好），同时报告"单配置对单配置"；
  2. **经典外部基线**：(a) HEFT / CPOP 列表调度在算子 DAG 上（每算子一子图，同一核内按列表序），这是多核 DAG 调度最常被引用的方法；
     (b) 多级图划分（"粗化—初始划分—细化"，能装上 `pymetis` 就用它，装不上就用自己写的简化版）+ 同步层次分层；
  3. **外部参照**：同学论文的 5 个用例结果作为"已知更强方法"的参照列，诚实报告差距以及 C1 之后的变化；
  4. **随机基线多种子**：官方 stub 跑 5 个种子，报告均值 ± 标准差。
- 依据：§2.6；现有 4 个基线本质上是"不分层的 CAP-LS"。
- 论文受益：§10.4 从"和自己的弱化版比"变成"和经典方法、同预算比"，这是评审最看重的一节。
- 验收：每个基线报告 N=4 算术平均 + 95% CI + 与 CAP-LS 的配对检验和胜/平/负。
- 依赖：D3；HEFT 与 C1 的表调度可以共用代码。

**D2　消融体系重建（三层）**　｜ P1 ｜ 1 天，大部分用已有数据
- 做什么：
  1. **机制层**：在最强单配置（c2）上重做一次逐项关闭（现在做在 c0 上）；增加交互项（`no_level × no_comm`），以及 SCC 合并、σ 线性扩展、`max_ops` 这三个目前没有消融的机制；
  2. **组合层**：留一候选消融 + 贪心曲线（§2.5，已有数据可直接算），外加 B1–B4 各保底项的贡献；
  3. **择优层**：纯代理择优（完全不调用官方评估器，也就是"算法本身"的结果）对比官方评估择优。这个数回答"你的算法离开评估器还剩多少"，是评审一定会问的问题。
- 依据：§2.2、§2.5；现有消融的 `full`（N=4 P2 2.996）比实际提交的组合（3.262）低 8%，两者不是同一个算法。
- 论文受益：§11.1。
- 验收：每个变体报告 N=4 算术平均、全 N 合并、配对 Wilcoxon p 值（Holm 校正）、胜/平/负。
- 依赖：D3；C2 做完后局部搜索那一行要重跑。

**D3　统计规范**　｜ P0 ｜ 0.5 天，不跑实验
- 做什么：写一个统一的汇总函数，所有比较一律输出：算术平均（主）、几何平均与中位数（辅）、对用例重采样 1 万次的 bootstrap 95% CI、
  配对 Wilcoxon 符号秩检验（多重比较用 Holm 校正）、胜/平/负计数（±0.1% 算平）；主结果再按 ρmax 三层分别报告。
- 依据：目前全文没有 CI 和检验。现有消融里 `no_localsearch` 在 P2 的全 N 配对 p=0.62、胜负 47:51，说明它和 `full` 没有差别；
  而 `no_balance` 胜负 137:131 却 p=0.0006，说明差异集中在少数大幅度用例上。这两种情况都需要统计量才能说清楚。
- 论文受益：§10、§11 的每一张表。
- 验收：`digest.py` 和 `fill_paper.py` 输出上述字段；正文每个"更好/更差"的结论后面都有 p 值或 CI。

**D4　样本规范化**　｜ P1 ｜ 0.5 天规划 + 按阶段重跑
- 做什么：
  1. 代理检验：从 40 个用例 × N=4 扩到 100 个用例 × N=2..5（命中缓存，零新评估）；
  2. 粒度实验：P2/P3 直接在 100 个用例上做（P2/P3 评估便宜），P1 在 C3 之后也做全量，做不到就用下面的分层抽样；
  3. 需要子采样的实验统一用**分层随机抽样**：按 ρmax 三层 × 规模三档分层，固定随机种子，在论文里写出抽样规则和种子；
  4. 论文里删掉"代表性用例"这类措辞。
- 依据：`validate_model.py:190` 只取前 40 个；`run_all.sh` 中粒度 25 个、敏感性 18 个用例是手工列出的。
- 论文受益：§10.7、§11.2、§11.3，消除选择性取样的质疑。
- 依赖：C3（P1 部分）。

**D5　超参数过拟合检验**　｜ P2 ｜ 0.5 天，用已有数据
- 做什么：β 默认值和候选网格都是在同一批 100 个用例上定的。做一次 2 折（或 5 折）检验：在一半用例上选 β 和候选子集，在另一半上评估，
  报告"折内选择 vs 全量选择"的差距。
- 依据：粒度实验显示最优 β≈0.25，而默认用 0.35；候选网格由经验定出。
- 论文受益：§11.2 可以写"参数选择不依赖测试集"，这在比赛论文里很少见，是加分项。
- 验收：折外损失 < 1%。已有 main + granularity 数据基本够用。

**D6　界与参照体系**　｜ P1 ｜ 0.5–1 天
- 做什么：
  1. 报告逐用例的 MK/LB 分布（CDF 图），不只报几何平均；
  2. 引入"单核归一化效率" (LB_N/MK_N)/(LB_1/MK_1)：单核方案自己就是 LB 的 1.15 倍（中位，均值 1.24，最大 2.08），这部分不是多核调度能消除的，应该扣掉；
  3. 逐用例的"可达加速比上界" MK_1/LB_N，报告"达到上界的比例"；
  4. **已知最优参照**：在分层抽样的 15 个用例 × N=4 上，用大预算（例如每配置 200 次官方评估的随机重启 + 邻域搜索）求一个经验最优，报告最终方案与它的差距；5 个共同用例再加同学的结果作参照；
  5. 挑 4–6 个代表结构（链式同步 case_016、Transformer 型 case_003、广播型 case_044 等）给出结构性上界，仿照同学论文的写法。
- 依据：目前 N=4 P2 有 23 个用例 MK/LB>2，有 31 个 <1.1；LB 的决定项 69/100 是 M，说明它主要是计算下界；论文"剩余差距主要来自同步与不可整除"的说法没有实验支撑。
- 论文受益：§10.7 下界段；也直接回答"结果离最优有多远"。
- 依赖：第 4 点的大预算搜索依赖 C3（P1）。

**D7　时间预算与可复现**　｜ P1 ｜ 1 天
- 做什么：
  1. 报告端到端时间（算法 + 择优评估）的分布，并单独给出 P1 大图；
  2. 画"任意时刻曲线"：按候选评估顺序，最好解随评估次数的变化，证明预算可控；
  3. 补 `requirements.txt`（Python 与依赖的版本），在 Linux 上完整跑一遍并记录耗时；
  4. 写 `verify_final.py`：对 `results/final_plans` 的全部方案逐一调用官方 CLI，比对附录表格的 Makespan、搬运量、命中率，输出不一致清单；
  5. 固定并记录所有随机种子，附录里写清楚。
- 依据：P1 端到端最长 6329 s；环境只在 Windows 上验证过；没有最终方案的官方 CLI 复核脚本（论文只报告了 case_019 一个用例的逐位一致）。
- 论文受益：§10.6、附录 B；直接回应赛题"附件：提供可复现的 Python 程序脚本"。
- 验收：`verify_final.py` 0 处不一致；全部用例端到端在 10 分钟内（依赖 C3）。

**D8　问题 3 的 L2 专项实验**　｜ P1 ｜ 1 天，约 5 CPU·h
- 做什么：赛题要求"研究其容量和带宽对多核切图与调度的影响"，这是 P3 的核心交付，现在是最薄弱的一块。
  1. **两种协议都做**：(a) 固定最终方案、只改 L2 参数，隔离硬件效应；(b) 改参数后重新优化，体现算法的适应性；
  2. **样本**：按 N=4 的 L2 收益分三层（>1.05 的 8 个全取；1.01–1.05 的 19 个抽 10 个；≤1.01 的 73 个抽 10 个），固定种子；
     容量取 0（即关闭）/64K/256K/1M/4M/16M，带宽 60/125/250/500/1000，N=2、4、5；
  3. **记录"对切图的影响"**，而不只是 Makespan：最优方案的子图数、跨核字节、θ、胜出候选随 L2 参数的变化；
  4. **命中来源拆分**：命中字节拆成"跨核多播"和"换入复读"（v2 的 3-2），回答"L2 帮的是多核共享还是给 spill 兜底"；
  5. **系统级与同方案两种 L2 收益都报告**：同一方案开关 L2 的比值（现在的做法），以及 P3 冠军对 P2 冠军的比值；
  6. `cachemodel.py` 接入：预测命中率与官方命中率的散点图（对应 A2 #4）；
  7. 补一个 θ 扫描（对应 A2 #3）。
- 依据：现有敏感性样本最大 L2 收益 1.066，而全体中 case_044 1.726、case_080 1.372、case_092 1.311、case_046 1.209、case_057 1.138 都不在样本里；
  基于这个样本得出的"继续加大只读 L2 不会带来收益"（§11.3 第 3 条）很可能是样本造成的。
- 论文受益：§8、§10.5、§11.3，P3 这一问从"一条结论"变成"分层的、机制可解释的结论"。
- 验收：高收益层单独给曲线；结论按层表述。
- 依赖：D4 的抽样规则；命中来源拆分不依赖其他项。

### 第 4 层　图表与论证（P2）

**E1　图表增补与改造**　｜ P2 ｜ 1 天
- 主结果图 fig05 加 95% CI 误差棒，纵轴写明"算术平均"；
- 新增：逐用例核数单调性热图（B3 后应全绿）；候选数—加速比贪心曲线（C6）；任意时刻曲线（D7）；
  P2 对 P3 的逐用例配对散点；L2 收益对 `multi_reader_bytes` 的分层散点（D8）；
  额外搬运量拆分堆叠柱（边界搬运 vs spill，§2.7）；代理检验改为用例内秩相关分布 + Recall@K 柱（A3）；MK/LB 的 CDF（D6）；
  基线胜/平/负条形图（D1）。
- 现有图 13（Pareto）按 A2 #6 改标题和解读。

**E2　论断—证据对照表（附录）**　｜ P2 ｜ 0.5 天
- 把正文每一条机制性或定量断言列成一行：断言、证据（表/图/文件:行号）、样本、统计量。写作时逐条自查，评审读起来也会觉得扎实。

**E3　算法叙事与数据对齐**　｜ P2 ｜ 随 C6
- 论文主线从"LPT + 局部搜索"改为"构造性可行性（分层）+ 通信感知原子化 + 核内全序表调度（C1）+ 多起点官方择优"，
  消融和贪心曲线会直接支持这条主线。负面结论（LPT 不如轮转、λ 项≈0）继续如实报告，这是加分项。

### 第 5 层　展望（P3，只写不做）

- 超限原子的切口 DP（12/100 个用例适用，且与难例无关，见 D4 裁定）；
- 无环超图划分替换 S1（(λ−1) 目标已精确，但边界搬运只占 26%）；
- 层次穿插（相邻层之间保持依赖合法的局部交换）；
- 多级无环图划分（dagP）作为 S1 的可选后端；
- 算子内切分（改变输入图，不符合提交格式）；
- P3 首读错峰（依赖 C1，分数收益小，可在 D8 有余力时做一个 5 用例的小实验）。

---

## 五、执行顺序与算力预算

**算力基线**（来自现有 CSV 的 runtime_s + eval_s 求和）：main 阶段 P1 35.6 CPU·h、P2 2.3、P3 2.35；ablation 46.2；granularity 15.5；
baseline 5.4；p3_compare 2.8。P1 占了大头，所以 **C3 是所有 P1 相关实验的前置条件**。

| 阶段 | 内容 | 依赖 | 新增算力 | 产出 |
|---|---|---|---|---|
| **第 1 阶段**（第 1 天） | A1–A4、D3、B1–B4 | 无 | 基本命中缓存 | 口径合规、成绩 +0.8～3.5%、min≥1、N 单调、P3≥P2、统计规范 |
| **第 2 阶段**（第 2 天） | B5；C3 开发与逐字节验证；C2 开发与离线验证（扩大到 100 例 × 4 个 N） | 第 1 阶段 | P2/P3 约 5 CPU·h | P2/P3 大图跑满；P1 可以重跑；代理检验结论 |
| **第 3 阶段**（第 3–5 天） | C1 试点（20 例）→ 全量；D1 基线；D2 消融的已有数据部分；D4、D8 | C2、C3 | 约 20–30 CPU·h | 新主结果；基线、消融、P3 专项 |
| **第 4 阶段**（第 6–7 天） | C4、C5、C6；D2 需重跑的部分；D5、D6、D7 | C1–C3 | 约 20 CPU·h | 精修收益、P1 新候选、界与参照、可复现 |
| **第 5 阶段**（第 8 天） | E1–E3、`finalize.sh`、论文重装配 | 全部 | — | 终稿 |

**关键路径**：B 层 → C3 → C1 → D1/D2 → E。C2 可以和 C3 并行开发。

**时间只有 2–3 天时的最小集**：A1–A4、D3、B1–B5、C1（只做 P2/P3）、D2 的组合层和择优层（只用已有数据）、D8 的命中来源拆分和分层样本。
这样论文没有硬伤、成绩拿到手里已有的最好值、有一个真正的新机制，P3 的结论也不再依赖一个漏掉高收益用例的样本。

---

## 六、做完之后论文应该能写出的句子（验收总表）

| 论文位置 | 目标表述 | 对应项 |
|---|---|---|
| §10.2 | "100 个用例、N=2..5 的算术平均加速比为 …（95% CI …）；**逐用例**加速比均 ≥1 且随核数单调不降" | A1、B1–B4、D3 |
| §10.2 | "P3 在全部 400 个配置上不劣于 P2" | B2 |
| §10.3 | "按 ρmax 分层，最难一层的平均加速比从 … 提升到 …（主要来自核内全序调度）" | C1 |
| §10.4 | "在相同评估预算下，CAP-LS 相对 HEFT / 多级划分 / 最强消融基线分别提升 …（配对 Wilcoxon p<…，胜/平/负 …）" | D1 |
| §10.5 / §11.3 | "L2 收益高度依赖输入共享结构：高收益层的容量曲线在 … 处饱和，低收益层基本平坦；命中中 x% 来自跨核多播，y% 来自换入复读" | D8 |
| §10.6 | "全部用例端到端在 X 分钟内完成；4 个候选即可达到完整组合 98% 的效果" | C3、C6、D7 |
| §10.7 | "新时间模型的用例内秩相关中位数由 0.48 提升到 …，Recall@3 由 48% 提升到 …；完全不调用评估器时的算法结果为 …" | A3、C2、D2 |
| §10.7 | "单核归一化后，N=4 的平均效率为 …；在 15 个抽样用例上距大预算经验最优平均 …%" | D6 |
| §11.1 | "分层与亲和原子化是主要贡献来源（p<…）；LPT 不如轮转、λ 项贡献不显著（如实报告）；核内全序调度贡献 …" | D2、E3 |
| §11.2 | "β 与候选集在 2 折交叉下的折外损失 <1%" | D5 |
| 附录 | "全部 1200 + 400 个方案经官方 CLI 复核，0 处不一致" | D7 |

---

## 附录：本文数字的复算方法

所有数字来自仓库当前 `results/` 下的 CSV，用 pandas/scipy 重新计算，没有运行新的官方评估：

- 冠军：`main.csv` 中可行记录按（case, problem, num_cores）取最小 Makespan；加速比 = `baseline_makespan / makespan`。
- 已知最优：`main / baseline / ablation / granularity / p3_compare` 五个 CSV 的可行记录，按（case, eval_problem, num_cores）取最小。
- 保底池估计：已知最优 → 并入单核基准 → 按 N 递增取 min(N, N−1)；P3 另与 P2 已知最优取 min（保守替代）。
- 单调性：N 核冠军 / N−1 核冠军 > 1.01 的配置。
- 消融统计：`ablation.csv` 按（case, N）配对，对 log(变体/full) 做 Wilcoxon 符号秩检验（零差值剔除），胜负以 ±0.1% 为界。
- 代理检验：`model_validation.csv`，每个用例内按候选去重后计算 Spearman、按估计值排序的 Top-K 召回与选择损失。
- 超限原子：对 100 个用例运行 `partition.make_blocks`（N=4）与 `affinity_atoms`，统计计算量超过 β·W/N 的原子（只读分析，不涉及评估器）。
- 下界：`plots.lower_bound` 同一公式，逐用例计算并记录决定项。
- 时间：`main.csv` 中各候选 `runtime_s + eval_s` 按配置求和。

复算脚本在本讨论串的临时目录中，没有提交到仓库；需要的话可以整理成 `solution/audit.py` 放进仓库。
